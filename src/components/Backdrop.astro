---
/**
 * 全局背景层。冷色平原 + 三团柔光斑 + 地平线光带 + 体积雾，纯 CSS，零图片素材。
 *
 * 为什么要单独抽一层、还固定定位：
 * 地平线的光必须相对视口静止，页面滚动时光才像"远处的天光"而不是贴纸；
 * 顺带也省掉了每次滚动重绘一大片渐变。
 *
 * 六层各司其职（全站通用，滚动到任何一页都能感觉到）：
 *   plain  —— 地平线上方的大气渐变，冷蓝灰，是整站的基调
 *   bloom  —— ★ 三团柔光斑（冷 / 暖 / 核心），见下方说明，这是本轮新增的一层
 *   warm   —— 贴在地平线位置的暖光晕，唯一暖色，越往下越淡
 *   ground —— 越往下越暗的地面，让长页面读起来有"站在平原上"的纵深感
 *   grid   —— 只在上方可见的极淡底纹，工程感（刻意压得很轻）
 * 颗粒单独放 body 的最后一层（z-index 最高），是为了让它像胶片一样覆盖全画面。
 *
 * ★ 为什么要加 bloom 这一层（本轮改版的核心判断）：
 * ------------------------------------------------------------
 * 毛玻璃要"看起来像玻璃"，前提是**它背后有东西可模糊**。
 * 光靠 plain + warm 这两层，全站的光都堆在视口底部（warm 是 bottom: -14rem），
 * 首屏中段基本是一片平的深色——玻璃 panel 压在这种地方，
 * backdrop-filter 无论开多大都揉不出差别，看起来就是"一块半透明灰板"，
 * 于是整屏显得又空又平（用户的反馈正是"很单调，没什么东西"）。
 *
 * 三团光的**分工**（只用一团是治不好的，这一点实测过）：
 *   cool  —— 大范围冷光，偏左上，负责给首屏定调、把深色背景"抬"起来
 *   warm  —— 大范围暖光，落在中下部，与 cool 交叠出一道冷→暖的横向过渡
 *   core  —— 小、亮、**模糊更浅**的核心光，稳定压在玻璃正后方
 *
 * 前两团是"氛围光"，梯度平缓；只有 core 那一团是"结构光"——
 * 因为**玻璃的可见度 ≈ 背后明暗梯度的陡峭程度**：
 * 只有平缓的大光斑时，玻璃透过去仍是一片均匀灰蓝，照样不像玻璃。
 */

---

<div class="backdrop" aria-hidden="true">
  <div class="backdrop__plain"></div>
  <div class="backdrop__bloom backdrop__bloom--cool"></div>
  <div class="backdrop__bloom backdrop__bloom--warm"></div>
  <div class="backdrop__bloom backdrop__bloom--core"></div>
  <div class="backdrop__warm"></div>
  <div class="backdrop__ground"></div>
  <div class="backdrop__grid"></div>
</div>
<div class="grain" aria-hidden="true"></div>
